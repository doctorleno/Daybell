"use client";
import {useState} from "react";
export default function DeleteAccount(){const [password,setPassword]=useState(""),[message,setMessage]=useState("");return <main className="signin-page"><div className="signin-card"><h1>Delete your Daybell account</h1><p>This permanently removes your account, sessions, events and to-dos from the active database. Export any plans you want to keep first. Cancel reminders on other devices before deleting; open those apps online afterward to clear their scheduled reminders. Provider disaster-recovery backups may retain prior data for their configured retention period.</p><a href="/login">Sign in first</a><form onSubmit={async e=>{e.preventDefault();if(!confirm("Permanently delete your account and every saved plan?"))return;try{const r=await fetch("/api/account",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});const data=await r.json() as {error?:string};setMessage(r.ok?"Your account and plans have been deleted.":data.error??"Could not delete account.");}catch{setMessage("Could not delete. Try again.");}}}><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label><button>Delete account permanently</button></form><p role="status">{message}</p></div></main>}

